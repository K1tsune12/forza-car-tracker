# Python
__pycache__/
*.pyc

# Virtual environment
.venv/

# PyInstaller build artifacts
build/
dist/
*.spec

# App-generated user data (created next to the app at runtime):
# per-game progress (owned_fh6.json, owned_fh5.json) and the legacy owned_cars.json
owned_*.json
settings.json

# Lint / test caches
.pytest_cache/
